# prod overlay 注入:nginx 把 /grafana 反代到 Grafana 子路径部署。
# 第一道门 = basic_auth(.htpasswd),第二道门 = Grafana 自身登录 —— 任一凭据单独泄露都进不去。
location /grafana/ {
    auth_basic "Restricted - Grafana";
    auth_basic_user_file /etc/nginx/.htpasswd;

    proxy_pass http://grafana:3000;      # 无尾斜杠:保留 /grafana 前缀,配合 GF_SERVER_ROOT_URL
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    # Grafana live 功能(websocket)
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";

    # 不继承 server 级 CSP:Grafana 页面自身管理安全策略,SPA 的严格 CSP 会破坏其面板渲染
    add_header X-Content-Type-Options nosniff always;
    add_header X-Frame-Options DENY always;
    add_header Referrer-Policy strict-origin-when-cross-origin always;
}
